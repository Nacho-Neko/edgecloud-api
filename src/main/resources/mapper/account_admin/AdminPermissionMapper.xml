<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mikou.edgecloud.account_admin.infrastructure.persistence.mapper.AdminPermissionMapper">

    <!-- 递归查询角色及其所有父角色的权限 -->
    <select id="selectByRoleIdWithInheritance" resultType="com.mikou.edgecloud.account_admin.infrastructure.persistence.entity.AdminPermissionEntity">
        WITH RECURSIVE role_hierarchy AS (
            -- 基础查询：当前角色
            SELECT id, parent_id FROM admin_role WHERE id = #{roleId,jdbcType=OTHER}
            UNION ALL
            -- 递归查询：父角色
            SELECT r.id, r.parent_id
            FROM admin_role r
            INNER JOIN role_hierarchy rh ON r.id = rh.parent_id
        )
        SELECT DISTINCT p.*
        FROM admin_permission p
        INNER JOIN admin_role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id IN (SELECT id FROM role_hierarchy)
    </select>

    <!-- 根据角色ID查询权限列表（不含继承） -->
    <select id="selectByRoleId" resultType="com.mikou.edgecloud.account_admin.infrastructure.persistence.entity.AdminPermissionEntity">
        SELECT p.*
        FROM admin_permission p
        INNER JOIN admin_role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id = #{roleId,jdbcType=OTHER}
    </select>

</mapper>
